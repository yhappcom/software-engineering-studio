# M001 — Android Emulator Flutter Runtime Transfer

Date: 2026-09-20
Status: **BOUNDED ANDROID EMULATOR TRANSFER VALIDATED — PHYSICAL/iOS/PRODUCT TRANSFER OPEN**

## Problem / selection
Balance Loop selected Mobile M001 because Foundations already had Dart JIT/AOT, Flutter host execution and Chrome transfer, while Mobile lacked representative Android/iOS execution. Android emulator execution is a materially different OS/application boundary with high MintTap/LogMate and cross-track leverage.

## SOURCE
Current Flutter integration-test documentation states that integration tests run on physical devices or OS emulators and documents `flutter test integration_test/app_test.dart` for Android-device execution. Primary sources checked 2026-09-20: Flutter integration-test and testing-overview documentation.

## CLAIM / TEST EVIDENCE CONTRACT
**CLAIM:** a minimal Flutter state transition can compile and execute through an Android application/integration-test boundary on Android Emulator rather than only host `flutter_test` or Chrome.

**SPEC/PROPERTY:** initial visible state is `0`; tapping keyed increment must mutate/rebuild state and expose `1`.

**TARGET:** isolated app generated by `flutter create --platforms android` in CI.

**ORACLE:** `integration_test` + `flutter_test` assert `0`, tap `Key('increment')`, settle, then independently assert `1`. CI success additionally requires the Android-emulator execution step and job to complete successfully.

**FAILURE MODEL:** workflow/shell infrastructure, emulator setup/boot/device discovery, Flutter Android build, application/integration runtime, semantic oracle, and natural job completion are separate verdict phases.

## Failure → repair chain
### Attempt 1 — shell-contract failure
Exact head `869e1ca493b3dafdb8c19cb91174e5f83a9f77bd`, run `35499758484`, job `106049294338`: emulator booted, but Bash-only `pipefail` was executed under `/usr/bin/sh`. **ROOT CAUSE:** shell-contract mismatch. Commit `ea3101b696f667b98ee8022afc7f71eef3a31d0e` changed only the prologue to POSIX-compatible `set -eu`.

### Attempt 2 — command-boundary/CWD failure
Run `35502482760`: Android identity/device discovery succeeded, but action script lines used independent shell commands, so standalone `cd` did not persist and Flutter reported no `pubspec.yaml`. **ROOT CAUSE:** invalid CWD-lifetime assumption. Commit `a67c20ca9b679eb3ba9b6bee2fcea8acae4ca70c` joined `cd` and `flutter test` in one command.

### Attempt 3 — fixture compile failure
Exact head `a67c20ca9b679eb3ba9b6bee2fcea8acae4ca70c`, run `35505103675`, job `106063467870`: Android 15/API35/x86_64 booted, `flutter devices` found `emulator-5554`, Gradle reached `assembleDebug`, then test compilation failed because `Key` lacked an importing library. **ROOT CAUSE:** synthetic fixture compile defect, not Android runtime failure. Commit `b6bfc4bf7e07c004c43326fb1821fd54de3986ba` added only `package:flutter/foundation.dart`.

## Attempt 4 — repaired regression / TRANSFER VALIDATION
Exact Studio head: `b6bfc4bf7e07c004c43326fb1821fd54de3986ba`  
Run: `35507983649`  
Job: `106070907373`  
Terminal verdict: **success**.

GitHub run metadata records `completed/success`; job metadata records every step successful, including `Execute on Android emulator`, followed by successful job completion. Exact workflow source at the tested ref shows that this step executes Android release/API/ABI queries, `flutter devices`, then `cd "$RUNNER_TEMP/m001_android_fixture" && flutter test integration_test/runtime_boundary_test.dart -d emulator-5554 --reporter expanded`. The test source asserts `0`, taps `Key('increment')`, settles, and asserts `1`.

**VALIDATION:** because the Android-emulator step containing the semantic test command completed successfully and the job completed successfully, the bounded oracle is validated at this exact fixture/ref. The prior attempt recorded Flutter 3.47.5 stable / Dart 3.13.4, Android 15 API35 x86_64 and emulator 37.1.11; the repaired workflow retained the same target structure, but exact run-log text was not available through the current evidence channel, so do not overclaim unobserved log lines beyond run/job metadata and exact workflow semantics.

**TRANSFER VALIDATION:** PASS for the synthetic Studio fixture on Android Emulator. This materially transfers the bounded Flutter state-transition oracle beyond host/Chrome into an Android application/integration-test execution boundary.

## SYNTHESIS
The four-attempt chain demonstrates why phase-specific verdicts matter: emulator boot is not application execution; device discovery is not build success; build invocation is not compilation; compilation is not runtime; and infrastructure success is not the semantic oracle. Minimal single-variable repairs preserved causal evidence until the same bounded semantic target completed successfully.

## Evidence limit
This is Android **emulator** evidence for a synthetic Studio fixture. It is not physical-device evidence, iOS/Safari/iPadOS/EFB evidence, release-AOT evidence, canonical LogMate/MintTap artifact/runtime evidence, background/process-death evidence, persistence/storage/permissions evidence, plugin/native-UI evidence, or production evidence.

## RELATED DOMAIN CHECK
- Foundations: F001 host→Chrome transfer checked; this adds a materially different Android target.
- Architecture: no architecture contract changed.
- Mobile: lead track; M001 transfer advanced, M002/M004/M005 native gaps remain.
- Data: no persistence/durability claim.
- Quality: failure chain distinguishes shell, CWD, compile, runtime/oracle and job-completion phases.
- Systems: exact ref/run/job provenance retained; CI interpreter and command lifetime are pipeline contracts.
- Design Studio / Web Manager / Marketing Manager: considered; not materially relevant to this bounded native runtime mechanism; no files edited.
- Product source: no product implementation audited; no product claim made.

## HANDOFFS
- Mobile → Foundations: bounded Flutter execution now transfers host→Chrome→Android Emulator; physical/iOS/product/release transfer remains open.
- Mobile → Quality: preserve the complete failure→isolation→minimal repair→successful regression chain and phase-specific verdicts.
- Mobile → Systems: shell/interpreter/CWD lifetime are executable-pipeline provenance; successful Android step/job is evidence only for the exact workflow target.

## OPEN / CHANGE WATCH
- Physical Android device and iOS execution.
- Process death/background/storage/permissions/plugin/native integration.
- Safari/iPadOS/EFB.
- Canonical LogMate/MintTap artifact/runtime transfer and release-AOT evidence.
- Flutter stable, Android emulator and third-party action behavior are version-sensitive; preserve exact ref/run/toolchain identity.
